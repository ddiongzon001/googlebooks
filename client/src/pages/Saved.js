import React, { Component } from `react`;

class Saved extends Compoent{

    state = {}

    render(){
        return(
            <h1>Hello this is the saved page</h1>
        )
    }
}

export default Saved;